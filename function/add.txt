

#include <math.h>
max
sqrt
pow

#include <string.h>
strcmp: 比较字符串

注释代码段（不参与编译）：
#if (0)
 ...
#end if

参与编译：
#if (1)
 ...
#end if

stdlib标准库库函数：
    atof() 字符串转化为float
    atoi() 字符串转化为int
    atol() 字符串转化为long int

    malloc()
    calloc()
    free()

    exit()
    system()

    rand()
    srand() //随机数种子

string.h标准库文件：
    strcopy(str, "Hello World");
    strcat(str, "hello");
